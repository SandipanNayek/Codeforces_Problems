#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n,m;
        cin>>n>>m;
        while(n--){
            int p;
            cin>>p;
            vector<int>arr(p);
            for(int i=0;i<n;i++){
                cin>>arr[i];   //input
            }
        }
        
     int count=0;
     vector<int>v(n);
    }
}
